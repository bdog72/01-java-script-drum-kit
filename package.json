{
  "private": true,
  "scripts": {
    "deploy": "surge ./public --domain 01-java-script-drum-kit.bdog72.surge.sh",
    "start": "browser-sync start --server 'public' --files 'public'"
  },
  "devDependencies": {
    "browser-sync": "^2.18.6",
    "stylelint": "^7.7.1",
    "stylelint-config-standard": "^15.0.1",
    "surge": "^0.18.0"
  }
}
